## 0 重构说明

本次重构重整全文章节顺序，**完全按照研发迭代排期、P0/P1/P2落地时序重构**，保留全部原始技术能力、交互稿、测试步骤、错误码、工时、验收标准；清理冗余重复内容、理顺模块依赖，解决原文档章节杂乱、优先级错乱、前后耦合歧义问题；全文适配研发迭代、联调、测试、上线、运维全流程查阅。迭代时序：P0首期30工作日→P1二期12工作日→P2远期按需迭代

## 1 文档基础信息

### 1.1 文档说明

- **编写角色**：产品经理
- **项目名称**：TestFlow（移动端自动化测试平台）
- **研发目标**：自研私有化部署移动端自动化测试平台，全覆盖Android、iOS真机测试能力，解决现有第三方测试工具收费高、设备调度受限、测试数据不外泄、多设备串行效率低问题；内置测试视频录制、日志溯源、测试报告、异常告警能力，赋能业务测试、研发自测、回归测试全流程
- **适用角色**：后端研发、前端研发、测试开发、运维、功能测试工程师
- **版本迭代**：V1.0 首期基础能力版本；本次重构全量需求划分业务优先级、拆分阶段性落地排期，消除研发范围蔓延
- **代码基线**：V1.0-code（2026-07），技术方案A已落地，详见第 16~20 章

### 1.2 术语定义

- 设备Agent：部署在测试服务器、真机代理端的 **Python 驻留进程**，负责设备注册、心跳上报、状态采集
- Python执行器：独立 **FastAPI 服务**，接收调度中心下发的脚本执行任务，负责 ADB/Appium 驱动与录屏
- 设备调度中心：平台核心模块（**Spring Boot 实现**），管控全部Android/iOS真机，实现设备锁抢占、并行排队、资源释放
- 并行测试：单条测试任务拆分，占用多台独立真机同时执行，互不抢占资源、互不干扰
- 屏幕录制：真机原生编码录制，同步采集操作画面、触控水印、系统弹窗，不依赖录屏APP
- WDA：iOS官方WebDriverAgent，适配iOS自动化驱动底层
- ADB：Android调试桥，安卓设备通信底层协议
- 控件池：平台自研可变控件资源池，归集高频变更UI控件，沉淀多维特征，适配APP月度迭代无序控件改动
- 私有控件绑定：步骤级专属控件隔离能力，单用例单步骤修改不全局生效，规避用例连锁报错

## 2 项目背景与痛点分析（前置立项，无优先级）

### 2.1 业务背景

公司现有移动端产品线包含电商App、办公IM App、线下业务App，覆盖Android 9及以上、**iOS 15及全部更高系统版本（无上限）**；当前依赖外部云测平台+零散本地自动化脚本，无统一测试中台，回归测试人力成本高，每周版本回归耗时12小时以上。为保障代码隐私、降低第三方服务费、提升测试效能，启动自研移动端自动化测试平台项目。

### 2.2 现存核心痛点

1. **设备管控混乱**：测试真机分散在各工位，无统一台账，设备离线、占用状态不可视，重复占用、资源闲置问题严重
2. **无法多设备并行**：现有脚本串行执行，单次仅能占用1台手机，批量回归效率极低
3. **问题溯源困难**：线上、测试偶现崩溃无完整录屏，仅有日志，无法还原操作现场，排查耗时久
4. **双端适配割裂**：Android、iOS自动化脚本语法不统一，测试人员学习成本高，无法批量下发任务
5. **数据安全风险**：第三方云测平台上传业务包、测试日志存在数据外泄风险，不符合内网安全规范
6. **月度迭代控件不稳定**：业务APP按月迭代频繁改动UI控件，自动化用例大面积失效，测试通过率无法稳定≥99%

## 3 全量需求分级&迭代总纲（全文核心，排期依据）

全量需求划分为三级优先级，严控研发范围，杜绝需求无序堆砌；优先级定义： P0：阻塞上线、必须首期交付，缺失则平台不可用，最高优先级（30工作日） P1：完善平台能力，不阻塞上线，首期收尾+第二个月度迭代落地（12工作日） P2：增值优化、远期能力，三阶段及后续迭代落地，可按需裁剪

### 3.1 全局硬性约束（全量P0，不可降级，贯穿全迭代）

- 部署约束：私有化内网部署，禁止外网联网，适配公司内网权限、堡垒机规则
- 系统兼容：**Android 9.0及全部更高系统版本（无上限）**；**iOS 15.0及全部更高系统版本（无上限）**；支持真机，暂不兼容模拟器；双向适配安卓、iOS官方后续新版本灰度迭代
- 性能约束：支持最大20台设备同时并行测试；单设备指令延迟≤300ms；录屏不占用超过15%手机CPU
- 安全约束：测试包、录屏文件、日志禁止外发；设备权限脱敏，禁止读取手机隐私数据；文件防篡改审计

### 3.2 P0首期刚需（V1.0上线阻塞性能力）

研发目标：完成平台底座+基础自动化能力，替代第三方云测基础能力，保障内网测试可用

1. **设备资源管理**：Android/iOS真机接入、状态上报、设备台账、基础远程控机
2. **多设备并行调度**：分布式设备锁、任务排队、资源释放、基础异常熔断（平台核心底座）
3. **双端自动化基础执行**：兼容Appium、Python脚本，统一执行入口，批量安装卸载APP
4. **全流程基础录屏**：任务启停录屏、水印加密、文件归档、基础回放
5. **基础日志采集**：系统日志、App崩溃日志聚合，异常日志留存
6. **基础测试报告**：执行结果汇总、关联录屏日志，PDF导出
7. **账号权限管理**：基础登录、角色分权、操作审计，满足内网安全合规

### 3.3 P1二期优化需求（稳定性迭代，通过率兜底）

研发目标：解决月度迭代稳定性、控件变更报错问题，固化测试通过率≥99%，补齐生产可用能力

- 设备运维能力：断线自动重连、Agent自愈、设备缓存清理、异常重启
- 控件适配底座：多层级AI元素定位引擎、页面快照差分比对，弱化控件ID强依赖
- 全局可变控件池：核心能力落地，自动归集高频变动控件，沉淀多维控件特征
- 步骤级私有控件隔离：单用例、单步骤控件修改隔离，禁止全局联动篡改
- 通过率治理：无效报错过滤、用例重试、分级兜底验收，稳住99%执行指标
- CI/CD基础对接：Jenkins流水线联动，触发自动化回归
- 文档+版本管控：全量文档迭代、代码版本联动、变更溯源流程落地

### 3.4 P2远期迭代需求（可裁剪增值能力）

研发目标：提质增效、平台产品化优化，不影响核心测试业务，排期后置

- 可视化用例编辑：拖拽编写用例，无需编码
- 精细化性能测试：CPU、帧率、流量、能耗全维度监控
- 测试数据Mock中台联动
- 控件池可视化运维看板、月度异动自动报表
- 信创环境深度适配、国产化硬件专项兼容

## 4 分级研发排期（按迭代时序，重构核心）

### 4.1 第一阶段：V1.0首期上线｜共计30工作日（仅落地全部P0）

1. 1~5工作日：环境搭建、基础架构、P0数据库建表、设备Agent底座开发
2. 6~14工作日：双端设备接入、远程控机、设备台账、白名单能力开发联调
3. 15~22工作日：调度引擎、分布式锁、并行任务、熔断能力开发（核心阻塞模块）
4. 23~27工作日：录屏、日志、测试报告、权限体系开发，安全合规自测
5. 28~30工作日：全链路压测、灰度上线、Bug闭环、交付归档

### 4.2 第二阶段：月度稳定性迭代｜共计12工作日（落地全部P1）

目标：解决通过率、控件迭代问题，固化自动化通过率≥99%，补齐生产稳定性

1. 1~3工作日：优化元素定位引擎、页面快照差分比对能力
2. 4~7工作日：开发全局控件池、数据双写、入池风控、版本隔离、熔断兜底
3. 8~10工作日：开发步骤级私有控件隔离、公私存储拆分、执行优先级适配
4. 11~12工作日：通过率降噪、分级验收改造、文档迭代管控闭环、全量回归

### 4.3 第三阶段：远期增值迭代｜按需排期（全部P2）

拆分多个轻量化月度迭代，按需裁剪，不占用核心研发人力，包含可视化用例编辑、性能测试、运维看板、信创深度适配全部能力

### 4.4 分级压力测试验收指标

- P0上线指标：20台并行运行4h稳定、录屏成功率≥99.5%、调度响应≤500ms
- P1迭代新增指标：控件整体识别成功率≥99.5%、控件池自身可用率≥99.9%、自动化业务通过率稳定≥99%

## 5 平台架构+技术方案（按开发时序）

### 5.1 四层分层架构（优先级拆分落地）

自上而下：前端展示层→业务服务层→调度引擎层→设备底座层；**落地优先级：设备底座(P0)→调度引擎(P0)→业务服务(P0)→前端可视化(P1)**

1. **前端展示层**：Web管理后台；基础页面P0，运维可视化、控件看板P2
2. **业务服务层**：用户、任务、报告、存储服务，全部P0；报表、异动统计P2
3. **调度引擎层（核心P0）**：设备调度、并行分发、异常熔断；控件适配引擎P1
4. **设备底座层（最高P0）**：ADB、WDA、Agent进程、录屏底层，优先开发，所有能力依赖底座

### 5.2 全局业务流程图【P0】

管理员接入真机→安装设备Agent→平台自动识别设备、录入台账→测试人员创建测试任务→筛选设备→调度分配→执行脚本→录屏+日志采集→生成报告→异常告警

### 5.3 技术栈选型（已落地版本｜方案A）

> **架构决策（2026-07）**：采用 **方案A — Spring Boot 调度中心 + Python 执行层**，兼顾企业 Java 主栈规范与测试团队 Python/Appium 生态；调度与执行分离，生产环境使用 Redis/MySQL/MinIO，本地开发可使用 H2 内嵌库零依赖启动。

#### 5.3.1 后端技术栈（调度中心）

- 主体框架：**Spring Boot 3.2 + Spring Security + JPA**（JDK 17）
- 模块职责：用户鉴权、设备台账、任务调度、分布式锁、报告聚合、控件池 API
- 中间件（生产）：**MySQL 8、Redis 7、MinIO**
- 中间件（本地开发）：**H2 文件库**（profile=local，免 Docker/MySQL/Redis）
- 通信协议：HTTP REST + JWT；设备 Agent 心跳/注册走 HTTP

#### 5.3.2 前端技术栈

- 框架：**Vue3 + Vite + Element Plus + Pinia**
- 可视化报表：**ECharts**（首页仪表盘 P0，专项看板 P2）
- 开发端口：3000；生产由 Nginx 反代后端 8080

#### 5.3.3 设备执行层（Python）

- **Python Agent**（`agent/`）：设备注册、2s 心跳、ADB/iOS 设备信息采集
- **Python 执行器**（`executor/`）：FastAPI 服务，接收调度中心下发的执行任务
- Android：ADB、screenrecord、uiautomator2（可选）
- iOS：WDA、libimobiledevice（可选，需 Mac 侧部署）
- 脚本兼容：**Python 原生脚本、Appium 脚本**（统一由执行器入口调度）

#### 5.3.4 架构拓扑（已实现）

```text
┌──────────────┐     ┌─────────────────────────┐     ┌──────────────────┐
│ Vue3 前端     │────▶│ Spring Boot 调度中心      │────▶│ MySQL / Redis    │
│ :3000 / :80  │     │ :8080 (锁/队列/API/报告)  │     │ / MinIO (生产)   │
└──────────────┘     └───────────┬─────────────┘     └──────────────────┘
                                 │ HTTP POST /api/v1/execute
                     ┌───────────▼─────────────┐     ┌──────────────────┐
                     │ Python 执行器 (FastAPI)  │────▶│ ADB / Appium     │
                     │ :9002                     │     │ 真机设备          │
                     └───────────▲─────────────┘     └──────────────────┘
                                 │ 设备注册/心跳
                     ┌───────────┴─────────────┐
                     │ Python Agent              │
                     └───────────────────────────┘
```

#### 5.3.5 原 Golang 方案说明（已归档）

- 目录 `backend/` 为初期 Go 原型，**不再作为主后端维护**；能力已迁移至 `backend-java/`
- 若后续有极致低延迟调度需求，可保留 Go 仅做调度层，Python 做执行层（方案C）

### 5.4 关键难点解决方案（按迭代优先级排序）

#### 5.4.1 P0阻塞级难点

- **多设备并行抢占冲突**：基于Redis Redlock实现分布式设备锁，上锁成功才可下发指令；任务退出、进程崩溃、断线自动释放锁，增加锁续命机制，防止误释放
- **iOS接入不稳定、WDA闪退**：平台内置自动重签WDA证书，断线自动重启进程；缓存设备授信信息，增加2s心跳保活
- **录屏占用手机性能过高**：25帧固定帧率、进程隔离、闲置休眠驱动，首期优先保障稳定性，画质优化延后P1
- **内网大视频存储拥堵**：MinIO分片存储；冷热归档基础能力首期落地，精细化压缩后置P1

#### 5.4.2 P1稳定性难点

**APP迭代控件频繁变更、用例失效**：搭建控件池+私有控件隔离双架构，平台全盘兜底，弱化研发编码约束，配套数据容错、熔断、脏数据清洗机制

### 5.5 接口设计约束【全量P0】

- 全部接口内网JWT鉴权，自动刷新
- 设备上报接口AES加密传输、参数加签
- 标准化对外开放API，对接流水线
- 统一分类错误码封装

## 6 数据库设计（分批次落地，对齐迭代）

### 6.1 P0首期建表（上线必须初始化）

设备信息表、测试任务表、录屏资源表、账号权限表、执行日志表

### 6.2 P1二期新增数据表

全局控件池主表、步骤私有控件绑定表、控件变更溯源日志表、用例自愈记录表

### 6.3 P2远期增值数据表

设备能耗统计表、用例拖拽编辑表、质量热力报表数据表

## 7 核心模块能力拆分（绑定迭代优先级）

### 7.1 P0首期核心模块（不可裁剪）

#### 7.1.1 设备管理模块

承担真机接入、基础运维，双端差异化适配，不可裁剪

- 设备接入：安卓双模式接入、iOS授信自动化
- 6类设备状态实时上报、台账管理
- 基础远程控机、应用安装卸载
- 设备白名单安全校验

#### 7.1.2 多设备并行调度模块

平台核心底座，禁止裁剪，全部首期落地

1. Redis分布式设备锁，防资源抢占
2. 任务优先级排队、队列管控
3. 并行拆分规则、进程隔离       同版本多机型并行、双端混合并行
4. 任务超时熔断、资源强制回收

#### 7.1.3 录屏能力模块

基础录屏首期全部落地；视频精细化压缩、回放倍速优化降级P1

- 任务联动启停录屏
- 双端原生编码、统一MP4输出
- 防篡改水印
- Android无ROOT录屏、iOS免越狱静默录屏

#### 7.1.4 自动化执行模块

- 主流自动化脚本兼容引擎
- 批量安装卸载应用
- 基础用例版本管理

#### 7.1.5 日志与报告模块

- 三合一日志聚合
- 报告联动录屏溯源
- 基础报告导出

### 7.2 P1二期专项模块（稳定性专项）

#### 7.2.1 控件适配专项模块

首期不上线，二期专项迭代，解决月度APP迭代控件失效、通过率下跌核心痛点

- AI多层级元素定位引擎
- 全局可变控件池+冷热淘汰、数据容错
- 公私控件隔离、步骤级私有绑定
- 控件池稳定性、脏数据风控、熔断兜底

#### 7.2.2 文档&版本管控模块

首期简化文档管控，二期补齐全量版本编号、变更审批、代码联动、巡检复盘机制，同步适配月度迭代节奏，约束文档与研发不割裂

#### 7.2.3 月度通过率保障体系

整合环境门禁、灰度熔断、用例自愈、报错降噪能力，兜底自动化执行通过率≥99%

### 7.3 P2远期增值模块

可视化用例编辑、性能监控、Mock中台、信创深度适配、质量热力看板

## 8 全局错误码定义（按迭代拆分）

### 8.1 P0基础错误码（首期上线）

- E1001：设备未录入白名单，禁止接入
- E1002：设备分布式锁占用，调度失败
- E1003：iOS WDA授信失效，连接中断
- E2001：设备录屏权限抢占失败
- E2002：视频分片上传超时
- E2003：存储空间不足，禁止启动任务
- E3001：任务队列溢出，拒绝新建任务
- E3002：任务超时熔断，强制终止

### 8.2 P1控件专项错误码（二期迭代）

- E4001：控件池检索异常，自动降级原生定位
- E4002：私有控件绑定失效，隔离校验拦截
- E4003：控件脏数据，禁止入库

## 9 UI/UX交互设计&交付（对齐研发排期）

### 9.1 全局基础设计约束【全量P0】

#### 9.1.1 视觉规范

- 主题配色：内网安全深蓝主色调，辅助灰、告警红、成功绿三色；禁止高饱和配色，降低运维视觉疲劳
- 布局版式：左侧固定导航栏+右侧业务主画布，全局统一栅格布局，页面跳转无布局抖动
- 兼容适配：1080P~4K内网显示器自适应，缩放无组件错位；适配国产化浏览器、旧版Chrome
- 字体规范：操作系统默认无衬线字体，规避外网商用字体版权风险

#### 9.1.2 通用交互规则

- 二次确认：删除任务、下线设备、清空录屏高危操作，强制弹窗二次校验
- 全局状态兜底：接口超时、服务熔断统一兜底404空白页，附带故障报错码，联动后端错误码
- 权限灰度：只读账号隐藏编辑、删除按钮；分级权限UI差异化渲染，禁止按钮置灰无效交互
- 加载状态：设备调度、录屏加载、报告生成增加骨架屏，消除空白卡顿感知

### 9.2 页面分级落地排期

#### 9.2.1 P0首期上线页面

核心：完成基础运维、设备管控、任务执行、报告溯源刚需页面，剔除所有可视化美化、增值页面

1. 登录&权限首页、设备管理页、测试任务管理页
2. 录屏与回放页、测试报告页、个人中心页

#### 9.2.2 P1二期迭代页面

绑定控件适配专项能力，补齐自动化运维交互，服务通过率治理、文档迭代管控

1. 控件池管理页、私有控件配置弹窗
2. 自动化通过率大盘、文档版本管理页、CI/CD联动配置页

#### 9.2.3 P2远期增值页面

- 可视化用例编辑器、控件异动热力看板
- 性能监控大屏、全局消息通知中心

### 9.3 核心页面UX业务逻辑+交互流程图

保留原有全部页面交互、状态联动、异常兜底逻辑；补齐P0/P1/P2分级流程图、前后端联调规则，联动全局错误码，作为前端交付唯一标准

### 9.4 UI验收标准&amp;交付物排期

- P0：低保真交互图+静态稿，首期归档；验收：兼容、权限、数据对齐100%生效
- P1：Figma高保真交付稿+交互原型；验收：控件隔离、数据统计、告警联动生效
- P2：可视化大屏动效原稿，远期归档

## 10 标准化测试操作手册（分级验收用例）

全文格式统一，对齐上线验收口径，分为P0刚需测试、P1稳定性测试、P2增值测试，包含录屏专项、业务App全量测试，联动错误码、复测SOP

### 10.1 P0首期验收测试步骤（上线准入）

#### 10.1.1 自动化录屏标准化测试步骤

适配双端原生录屏底层能力、前端交互规范、后端调度逻辑，分为自动化录屏、运维手动核验、异常复测，联动E2xxx专项错误码

#### 10.1.2 业务App基础测试步骤

- 业务功能自动化测试
- App崩溃异常捕获测试
- 安装卸载兼容性测试

### 10.2 P1二期稳定性测试步骤

- UI元素适配专项测试
- 业务弱网模拟测试
- 账号与会话隔离测试
- 版本差分回归测试

### 10.3 P2远期增值测试步骤

- 业务精细化性能测试
- 全链路Mock接口测试

### 10.4 平台底座自测分类（区分业务测试）

设备连通性、调度锁校验、Agent保活、存储自测属于**平台底座测试**，不属于业务App测试，首期自测闭环，禁止混入业务通过率统计

## 11 上线验收标准（分级卡点）

### 11.1 P0首期验收（上线硬性准入）

- Android9+、iOS15+全量机型稳定接入，版本升级无需二次适配
- 15台设备并行执行无抢占、无资源泄漏
- 双端录屏稳定、日志溯源完整、内网安全合规审计通过
- 基础页面UI渲染无误，权限分权、高危拦截交互生效

### 11.2 P1二期迭代验收（稳定性准入）

- 研发无序变更控件场景下，平台无需改造用例，自动适配变动元素
- 单步骤控件修改，不联动污染全局、其余用例，数据隔离100%生效
- 月度全量自动化执行通过率≥99%，剔除无效环境报错
- 控件池无脏数据、无跨版本串扰，故障可熔断回滚

### 11.3 P2远期验收标准

功能可用性、可视化便捷度、国产化适配专项验收，不做上线硬性卡点

## 12 风险评估+分级预案（绑定迭代）

### 12.1 P0阻塞级风险（首期优先兜底）

- 风险：iOS大版本迭代WDA适配失效；预案：底层驱动解耦、证书自动重签兜底
- 风险：高并发调度雪崩；预案：服务独立部署、队列备份、故障自动重启
- 风险：内网存储爆满；预案：自动清理阈值、冷热归档兜底

### 12.2 P1稳定性风险（二期专项兜底）

- 风险：控件池数据错乱污染、反向拉低通过率；预案：双写存储、哈希去重、快照回滚
- 风险：私有控件隔离失效、全局篡改；预案：三元绑定隔离、变更日志独立审计

## 13 运维部署&线上SOP

### 13.1 权限分级运维规范【P0】

- 超级管理员、测试管理员、测试人员、研发只读账号四级分权

### 13.2 部署模式说明

平台支持两种部署模式，按环境选择：

| 模式 | 适用场景 | 依赖 | 启动方式 |
|------|----------|------|----------|
| **本地开发** | 研发联调、功能验证 | JDK17 + Node + Python | `scripts/start-local.ps1` |
| **生产/Docker** | 内网正式上线 | Docker + MySQL + Redis + MinIO | `docker compose up -d` |

#### 13.2.1 本地开发部署（零 Docker）

**环境要求**

| 组件 | 版本 | 必须 |
|------|------|------|
| JDK | 17+（Temurin 推荐） | 是 |
| Node.js | 18+ | 是 |
| Python | 3.11+ | 是 |
| Maven | 3.9+（或使用项目内置 `tools/apache-maven-3.9.6`） | 编译时需要 |
| Docker | — | 否 |

**一键启动（PowerShell 推荐）**

```powershell
cd E:\自动化测试平台-1
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\start-local.ps1
```

**一键启动（CMD）**

```bat
scripts\start-local.bat
```

**访问地址**

- 前端：http://localhost:3000
- 后端健康检查：http://localhost:8080/api/health
- 执行器：http://localhost:9002/health
- 默认账号：`admin` / `admin123`

**本地 Profile 说明（`spring.profiles.active=local`）**

- 数据库：H2 文件库，路径 `backend-java/data/atp_local`
- 调度队列/设备锁：内存实现（无需 Redis）
- 录屏存储：本地目录 `backend-java/data/recordings`
- 启动时自动创建管理员账号与测试白名单设备 `local-test-device`

**手动分步启动**

```bat
REM 1. 编译并启动后端
cd backend-java
..\tools\apache-maven-3.9.6\bin\mvn.cmd -DskipTests package
java -jar target\atp-platform-1.0.0.jar --spring.profiles.active=local

REM 2. 启动 Python 执行器
cd executor
pip install -r requirements.txt
python main.py

REM 3. 启动前端
cd frontend
npm install && npm run dev

REM 4. 启动 Agent（测试机旁，可选）
cd agent
pip install -r requirements.txt
python main.py
```

#### 13.2.2 生产 Docker 部署

```bat
docker compose up -d --build
```

- 前端：http://localhost
- 后端：http://localhost:8080
- MinIO 控制台：http://localhost:9001
- 后端 Profile：`prod`（MySQL + Redis + MinIO）

**初始化数据库**

```bat
mysql -h localhost -u atp_user -patp_pass atp_platform < migrations\001_init_p0.sql
mysql -h localhost -u atp_user -patp_pass atp_platform < migrations\002_init_p1.sql
```

### 13.3 设备接入 SOP【P0】

1. 管理员登录 → 设备管理 → 添加白名单（填写设备序列号/UDID）
2. 测试机开启 USB 调试，执行 `adb devices` 确认可见
3. 修改 `agent/config/config.yaml` 中 `platform_url` 指向后端地址
4. 运行 Agent：`python agent/main.py`
5. 设备列表出现在线设备后，即可创建并提交测试任务

### 13.4 灰度上线分级（规避上线风险）

- 灰度一期：5台真机，内部试用，校验P0底座能力
- 灰度二期：12台设备，业务测试验收核心能力
- 灰度三期：全量20台上线，冻结P0能力，开启P1迭代排期

### 13.5 线上故障应急 SOP【P0】

| 故障现象 | 排查步骤 | 处理动作 |
|----------|----------|----------|
| 后端无法启动 | 查 JDK 版本、端口 8080 占用 | 换端口或释放占用进程 |
| 登录 401 | 确认 profile=local 时 admin 已初始化 | 删除 H2 库重启或重置密码 |
| 任务一直 pending | 查调度队列、设备是否 online | 提交任务后检查后端日志 |
| 设备 offline | Agent 是否运行、白名单是否添加 | 重启 Agent，检查心跳接口 |
| 执行器无响应 | 访问 :9002/health | 重启 executor，后端自动降级 mock |
| 录屏无法回放 | 生产查 MinIO；本地查 recordings 目录 | 确认录屏 status=ready |

**日志查看（Docker）**

```bat
docker compose logs -f backend
docker compose logs -f executor
```

### 13.6 常见部署问题（FAQ）

| 问题 | 原因 | 解决 |
|------|------|------|
| `chcp 不是内部或外部命令` | PATH 缺少 System32 | 使用 `start-local.ps1` 或新版 bat |
| `未找到 Java` | java 未加入 PATH | 设置 JAVA_HOME 或使用 ps1 脚本 |
| 批处理中文乱码/语法错误 | bat 编码与中文路径冲突 | 优先用 PowerShell 脚本启动 |
| `mvn 不是命令` | 未安装 Maven | 使用 `tools/apache-maven-3.9.6/bin/mvn.cmd` |
| 前端能开但登录失败 | 后端未启动 | 先确认 :8080/api/health 返回 ok |

## 14 非功能性需求（分级落地）

### 14.1 性能指标【P0硬性卡点】

- 页面首屏≤1.5s、看板刷新≤500ms
- 50人在线、10条任务并行无堆积
- 月度服务可用率≥99.95%

### 14.2 兼容性需求【P0】

- 主流内网浏览器兼容
- Linux、国产化服务器适配
- 基线系统降级兜底兼容

### 14.3 埋点监控【分级】

- 基础业务、服务监控P0；控件变更、质量埋点P1；可视化报表P2

## 15 交付物清单（按迭代归档）

- 首期交付：源码、部署脚本、P0数据库SQL、安全验收报告、运维手册
- 二期交付：控件池专项源码、新增数据表、适配文档、稳定性复盘报告
- 远期交付：增值能力源码、可视化运维资产

---

## 16 代码工程与目录结构（落地对照）

### 16.1 仓库目录

```text
自动化测试平台-1/
├── backend-java/          # 【主后端】Spring Boot 调度中心
│   ├── src/main/java/com/atp/platform/
│   │   ├── controller/    # REST API
│   │   ├── service/       # 业务/调度/执行器客户端
│   │   ├── entity/        # JPA 实体
│   │   ├── repository/    # 数据访问
│   │   ├── security/      # JWT 鉴权
│   │   └── config/        # 配置/本地初始化
│   └── src/main/resources/
│       ├── application.yml         # 默认 local profile
│       ├── application-local.yml   # H2 本地模式
│       └── application-prod.yml    # MySQL/Redis/MinIO 生产模式
├── executor/              # Python FastAPI 执行器（ADB/Appium/录屏）
├── agent/                 # Python 设备 Agent（注册/心跳）
├── frontend/              # Vue3 管理后台
├── migrations/            # MySQL 初始化 SQL（P0 + P1）
├── scripts/               # 启动与环境检测脚本
│   ├── start-local.ps1    # 本地一键启动（推荐）
│   ├── start-local.bat    # 本地一键启动（CMD）
│   └── check-env.bat      # 环境检测
├── tools/                 # 内置 Maven（免安装）
├── docs/                  # 补充文档
└── docker-compose.yml     # 生产容器编排
```

### 16.2 模块实现状态（截至 V1.0 代码基线）

| 模块 | 优先级 | 实现状态 | 代码位置 |
|------|--------|----------|----------|
| 用户登录/JWT/四级权限 | P0 | ✅ 已实现 | `AuthController`, `SecurityConfig`, `@PreAuthorize` |
| 设备台账/白名单 | P0 | ✅ 已实现 | `DeviceController`, `DeviceService` |
| 分布式设备锁/续期 | P0 | ✅ 已实现 | Redis（prod）/ 内存（local） |
| 任务队列/优先级调度 | P0 | ✅ 已实现 | `SchedulerService`, 优先级队列 |
| 双端/指定设备调度 | P0 | ✅ 已实现 | `DeviceService.resolveForTask` |
| 任务超时熔断 E3002 | P0 | ✅ 已实现 | `SchedulerService.runExecution` |
| 任务失败重试 | P0 | ✅ 已实现 | `SchedulerService.tryRetry` |
| Python 脚本执行 | P0 | ✅ 已实现 | `executor/main.py` |
| Appium 脚本执行 | P0 | ✅ 基础实现 | `executor/main.py` |
| ADB 录屏/水印/归档 | P0 | ✅ 已实现 | `executor/main.py`, `RecordingStreamController` |
| logcat 日志采集 | P0 | ✅ 已实现 | `executor/main.py` → `execution_logs` |
| 批量安装/卸载 | P0 | ✅ 已实现 | `DeviceController`, `PythonExecutorClient` |
| 测试报告生成 | P0 | ✅ 已实现 | `ReportService` |
| PDF 报告导出 | P0 | ✅ 已实现 | `PdfReportService`, `ReportExportController` |
| 录屏本地回放 | P0 | ✅ 已实现 | `RecordingStreamController`（local） |
| 录屏 MinIO 存储 | P0 | ✅ prod 已实现 | `MinioRecordingStorageService` |
| 控件池管理 | P1 | ✅ 已实现 | `ControlPoolService`, `Controls.vue` |
| 私有控件绑定 | P1 | ✅ 已实现 | `ControlController`, `TaskDetail.vue` |
| 控件运行时解析注入 | P1 | ✅ 已实现 | `ControlResolverService`, `executor/atp_controls.py` |
| 控件变更溯源 | P1 | ✅ 已实现 | `ControlChangeLog` |
| 用例自愈记录 | P1 | ✅ 基础实现 | `CaseHealingRecord`, pool_fallback |
| 通过率治理 | P1 | ✅ 已实现 | `SchedulerService`, `atp.governance` |
| AI 元素定位引擎 | P1 | ✅ 基础实现 | `ai_locator.py`, `AiLocateService` |
| Jenkins CI/CD 对接 | P1 | ✅ 已实现 | `CiController`, `CiSettings.vue` |
| 通过率大盘 | P1 | ✅ 已实现 | `Dashboard.vue` 趋势图 |
| WebSocket 投屏 | P1 | ✅ 已实现 | `screen_stream.py`, `DeviceScreen.vue` |
| 远程触控 | P1 | ✅ 已实现 | tap/swipe/input API |
| 可视化用例编辑 | P2 | ✅ 基础实现 | `VisualCaseEditor.vue`, `VisualScriptGenerator` |
| iOS WDA 自动重签 | P0 | ⏳ 待迭代 | Agent 扩展 |

图例：✅ 已完成　🔶 部分完成　⏳ 规划中

---

## 17 API 接口规范（已实现）

统一前缀 `/api/v1`；响应格式 `{ code, message, data, error? }`；`code=0` 表示成功。

### 17.1 认证接口

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/auth/login` | 否 | 登录，返回 JWT |
| GET | `/auth/profile` | 是 | 当前用户信息 |
| PUT | `/auth/profile` | 是 | 更新显示名称 |
| PUT | `/auth/password` | 是 | 修改密码 |

### 17.2 设备接口

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/agent/devices/register` | 否 | Agent 注册设备 |
| POST | `/agent/devices/heartbeat` | 否 | Agent 心跳 |
| GET | `/devices` | 是 | 设备列表（分页） |
| GET | `/devices/{id}` | 是 | 设备详情 |
| PUT | `/devices/{id}/status` | 管理员 | 更新设备状态 |
| DELETE | `/devices/{id}` | 管理员 | 下线设备 |
| GET | `/devices/whitelist` | 是 | 白名单列表 |
| POST | `/devices/whitelist` | 管理员 | 添加白名单 |
| DELETE | `/devices/whitelist/{id}` | 管理员 | 删除白名单 |

### 17.3 任务接口

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/tasks` | 是 | 任务列表 |
| POST | `/tasks` | 测试角色 | 创建任务 |
| GET | `/tasks/{id}` | 是 | 任务详情 |
| POST | `/tasks/{id}/submit` | 测试角色 | 提交调度 |
| POST | `/tasks/{id}/cancel` | 测试角色 | 取消任务 |
| DELETE | `/tasks/{id}` | 管理员 | 删除任务 |
| GET | `/tasks/{id}/executions` | 是 | 执行实例 |
| GET | `/tasks/{id}/logs` | 是 | 执行日志 |

### 17.4 录屏/报告/控件接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/recordings` | 录屏列表 |
| GET | `/recordings/{id}/playback` | 获取回放 URL |
| GET | `/recordings/{id}/stream` | 本地模式视频流（需 JWT） |
| GET | `/reports` | 报告列表 |
| GET | `/reports/{taskId}` | 报告详情（含日志/录屏） |
| GET | `/reports/{taskId}/export` | 下载 PDF 报告 |
| POST | `/devices/{id}/install` | 远程安装 APK |
| POST | `/devices/{id}/uninstall` | 远程卸载应用 |
| GET | `/dashboard` | 首页统计数据 |
| GET/POST | `/controls/pool` | 控件池管理 |
| GET | `/controls/lookup` | 控件池检索 |
| POST | `/controls/resolve` | 解析控件定位 |
| POST/GET/DELETE | `/controls/private-bindings` | 私有控件绑定 |
| GET | `/controls/healing-records/{taskId}` | 自愈记录 |
| POST | `/controls/ai-locate` | AI 元素定位 |
| GET/PUT | `/ci/config` | CI 配置 |
| POST | `/ci/jenkins/webhook` | Jenkins 触发（Token 鉴权） |
| GET | `/ci/tasks/{taskId}/status` | CI 任务状态轮询 |
| GET | `/ci/jobs/recent` | 最近 CI 触发记录 |
| POST | `/devices/{id}/screen/start` | 开始投屏（返回 WS URL） |
| POST | `/devices/{id}/screen/tap` | 远程点击 |
| POST | `/devices/{id}/screen/swipe` | 远程滑动 |
| POST | `/devices/{id}/screen/input` | 远程输入文本 |
| GET | `/screen/verify` | 投屏 Token 校验（执行器调用） |
| PUT | `/tasks/{id}` | 更新任务 |
| POST | `/tasks/visual/preview` | 预览可视化脚本 |

### 17.5 Python 执行器接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/api/v1/execute` | 执行自动化任务（由 Java 调度中心调用） |
| POST | `/api/v1/device/install` | 安装 APK |
| POST | `/api/v1/device/uninstall` | 卸载应用 |

**execute 响应字段**：`success`, `summary`, `recording_path`, `watermark_hash`, `file_size`, `logs[]`

---

## 18 任务执行全链路（研发联调参考）

```text
1. 用户在前端创建任务（Tasks 页面）
      ↓
2. POST /api/v1/tasks  →  任务 status=pending
      ↓
3. 用户点击「提交」→ POST /api/v1/tasks/{id}/submit
      ↓
4. SchedulerService 入队 → 分配可用设备 → Redis/内存加锁
      ↓
5. 创建 TaskExecution 实例 → HTTP 调用 Python 执行器
      POST http://localhost:9002/api/v1/execute
      ↓
6. 执行器：安装 APK（可选）→ 启动录屏 → 运行 Python/Appium 脚本
      ↓
7. 结果回写 → 更新 execution/task 状态 → 自动生成 TestReport
      ↓
8. 释放设备锁 → 前端报告页/任务详情页可查看日志与录屏
```

**并行执行规则**：`parallel_count` 决定同时占用的设备数；每台设备独立 execution，互不干扰；全部 execution 结束后汇总 pass_rate 生成报告。

---

## 19 数据库表与 SQL 文件对照

| SQL 文件 | 优先级 | 表 |
|----------|--------|-----|
| `migrations/001_init_p0.sql` | P0 | users, devices, device_whitelist, test_tasks, task_executions, recording_resources, execution_logs, test_reports, audit_logs |
| `migrations/002_init_p1.sql` | P1 | control_pools, private_control_bindings, control_change_logs, case_healing_records |

本地 H2 模式由 JPA `ddl-auto=update` 自动建表，无需手动执行 SQL。

---

## 20 版本记录

| 版本 | 日期 | 变更摘要 |
|------|------|----------|
| V1.0-doc | 2026-07 | 需求分级重构，原始 PRD 定稿 |
| V1.0-code | 2026-07 | 方案A落地：Spring Boot + Python + Vue3；P0 核心 API/页面/调度已实现；本地零 Docker 启动支持 |
| V1.0-p0 | 2026-07 | P0 闭环：超时熔断、双端/指定设备调度、优先级队列、录屏水印+logcat、PDF 导出、RBAC、安装卸载 |
| V1.0-p1 | 2026-07 | P1：控件池运行时注入、私有控件 UI、变更日志、自愈记录、通过率治理 |
| V1.0-p1b | 2026-07 | Jenkins Webhook、AI uiautomator 定位、通过率趋势大盘 |
| V1.0-p2 | 2026-07 | WebSocket 投屏+远程触控、可视化用例编辑器 |
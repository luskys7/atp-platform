## 1. 文档说明

**文档目的**：统一明确控件池按页面维度分组管理的产品需求、功能细则、前端交互规范、原型逻辑、验收标准，供产品、前端、后端、测试全员对齐，可直接用于开发排期与落地。

**适用范围**：自动化测试平台-控件池模块

**版本状态**：V1.0 正式交付版

## 2. 需求概述

### 2.1 需求背景

现有控件池已存在「页面」数据字段（page），可标记登录页、清洁首页、定时任务页、弹窗页等控件归属，但仅为静态展示字段，无筛选、分组、批量管理、页面运维能力。业务迭代中页面改版、控件维护、问题修复、版本导出均需要全局检索控件，效率极低且容易遗漏控件，多人协作无页面隔离，维护混乱。

### 2.2 需求目标

基于现有page字段，零重构成本实现**页面维度标准化管控**，实现控件按页面分组展示、精准筛选、批量运维、页面独立管理，彻底解决页面迭代维护难、控件杂乱、无范围隔离的问题。

### 2.3 受众角色

自动化测试工程师、脚本开发人员、平台管理员、测试运维人员

### 2.4 优先级与排期规划

- **P0 极速上线（1个工作日）**：页面筛选器、双视图切换、页面分组展示
- **P1 迭代优化（2个工作日）**：页面管理功能、控件批量分配页面、分组批量操作、规范页面录入
- **P2 深度能力（3个工作日）**：页面级批量修复、页面独立版本导出、页面依赖统计、页面数据汇总统计

## 3. 现状分析

### 3.1 现有能力

数据库已预留 page 页面字段，支持存储页面标识（Login、auto_page等）；控件池页面为平铺表格视图，展示页面字段文本信息，无任何聚合与筛选能力。

### 3.2 核心痛点

- 页面改版需全局检索对应控件，手动筛选效率低、易遗漏；
- 页面命名不规范，中英文混杂、自定义输入杂乱，无统一管控；
- 批量修复、版本导出、依赖查看均为全局维度，无法限定单页面范围；
- 无页面分组视图，不同页面控件混杂展示，多人协作无隔离；
- 零散无归属控件无法统一归集清理，冗余控件堆积。

## 4. 详细功能需求 & 前端交互原型

本章包含所有功能细则 + **文字交互原型**，前端可直接对照开发，无需额外UI图纸。

### 4.1 P0 基础能力：筛选 + 双视图 + 页面分组

#### 4.1.1 顶部筛选栏改造（交互原型）

**原有布局**：应用包名输入框 + 刷新按钮

**新增布局**：应用包名输入框 + 页面筛选下拉框 + 刷新按钮

**下拉框交互规则**：

- 默认占位符：「请选择页面筛选」；
- 数据源：自动拉取当前应用下所有page字段值，自动去重，同步展示中文别名；
- 筛选项包含：全部页面、未归属页面、登录页、清洁首页、定时任务页、协议弹窗页等；
- 支持**多选**，可同时筛选多个页面控件；
- 支持一键清空筛选，刷新页面后筛选条件保留/清空跟随用户操作；
- 刷新按钮点击后，同步更新页面筛选下拉数据源。

#### 4.1.2 双视图切换功能（交互原型）

**入口位置**：筛选栏右侧，新增两个按钮切换组件【平铺视图】【页面分组视图】，默认选中「平铺视图」。

**视图1：平铺视图（保留原有全部逻辑）**

完全沿用现有表格样式、列字段、操作逻辑，页面筛选器可正常生效，无改动。

**视图2：页面分组视图（核心新增）**

控件自动按页面字段分组展示，整体折叠布局，交互规则如下：

- 分组标题模板：「展开/折叠箭头 + 页面中文别名（英文标识） | 控件总数：XX | 总命中次数：XX」；
- 默认所有分组**折叠状态**，用户点击箭头可单独展开/折叠单组；
- 无page值的控件统一归入【未归属页面】分组，分组底色设置浅灰色，视觉区分；
- 每个分组头部固定展示批量操作按钮：【批量编辑】【批量分配页面】【批量标记失效】；
- 展开分组后，内部控件沿用原有表格列样式，字段、操作按钮完全不变。

**原型示例展示**

> ▶ 登录页（Login） | 控件总数：12 | 总命中次数：328
>
> ▶ 清洁首页（home_page） | 控件总数：25 | 总命中次数：890
>
> ▶ 定时任务页（task_page） | 控件总数：18 | 总命中次数：456
>
> ▶ 未归属页面 | 控件总数：6 | 总命中次数：32

### 4.2 P1 进阶能力：页面管理 + 批量分配 + 录入规范化

#### 4.2.1 页面管理弹窗（交互原型）

**入口位置**：筛选栏右侧，视图切换旁，新增【页面管理】按钮

**弹窗结构**：标题、页面列表表格、底部关闭按钮

**表格列字段**：页面英文标识、页面中文别名、关联控件数、操作

**支持操作交互**：

- **新增页面**：弹窗输入唯一英文标识、自定义中文别名，提交后同步至下拉筛选数据源；
- **编辑别名**：仅可修改中文展示名，英文标识不可修改（保证数据唯一稳定）；
- **合并页面**：选中源页面，选择目标页面，二次确认后，源页面所有控件批量迁移至目标页面，源页面保留/可删除；
- **清空页面**：一键清空当前页面所有控件的page字段，全部移入未归属页面；
- **删除页面**：仅无关联控件时可删除，有控件则弹窗提示「该页面存在关联控件，无法删除，请先清空」。

#### 4.2.2 控件批量分配页面（交互原型）

- 表格支持多选控件，顶部通用批量操作栏新增【分配页面】按钮；
- 点击后弹出下拉选择框，可选择已有页面，或直接新建页面并绑定；
- 提交后批量更新所有选中控件的page字段，实时刷新表格分组数据；
- **录入规范优化**：新增控件、编辑控件弹窗中，原页面自由输入框改为**下拉选择框**，禁止手动输入，彻底统一命名规范。

### 4.3 P2 深度运维能力：页面级专项功能

#### 4.3.1 页面级批量修复

右上角【批量修复】弹窗新增「页面筛选」条件，支持单选/多选页面，仅对选中页面的控件执行批量修复，隔离全局控件，避免误操作。

#### 4.3.2 页面独立版本导出

原有版本导出功能新增分支：支持「全局导出」「当前筛选页面导出」，可单独导出登录页、定时任务页等单页面全量控件定位规则，用于页面迭代单独归档。

#### 4.3.3 页面级依赖统计

每个页面分组头部新增【依赖查看】入口，点击弹窗展示当前页面**所有控件**的引用关系，包含关联用例、关联脚本数量及明细，页面改版前可快速评估影响范围。

#### 4.3.4 页面数据汇总统计

分组头部实时统计展示：页面控件总数、有效（active）控件数、失效控件数、总命中次数，直观展示页面控件使用状态与稳定性。

## 5. UI&交互统一规范

- 折叠交互：折叠状态图标「▶」，展开状态图标「▼」，全局统一；
- 视觉区分：未归属页面分组浅灰色底色，提醒用户规整控件归属；
- 命名规范：后端存储唯一英文key，前端展示中文别名，兼顾代码稳定与用户体验；
- 操作兜底：所有批量迁移、清空、删除操作均增加二次确认弹窗，防止误操作；
- 筛选联动：所有运维功能默认联动当前页面筛选条件，精准限定操作范围。

## 6. 数据层改造方案

- 无需改动原有控件数据表，完全复用现有 page 字段，无重构成本；
- 新增页面配置表：字段包含应用包名、页面英文唯一标识、页面中文别名、创建时间；
- 所有批量操作增加事务机制，批量迁移、清空页面数据支持异常回滚；
- 页面下拉数据源从页面配置表读取，统一管控，杜绝自定义杂乱输入。

## 7. 分阶段验收标准

### 7.1 P0 验收标准

- 页面筛选下拉框正常加载、支持单选/多选/清空筛选，筛选结果精准；
- 双视图切换正常，平铺/分组视图互不冲突；
- 页面分组折叠/展开交互正常，统计数量、命中次数数据准确；
- 无归属控件自动归集为未归属分组，视觉区分正常。

### 7.2 P1 验收标准

- 页面管理弹窗新增、编辑、合并、清空、删除功能全部正常；
- 控件多选后可批量分配页面，数据实时更新；
- 新增/编辑控件页面为下拉选择，禁止手动输入，命名规范化；
- 分组头部批量操作功能正常生效。

### 7.3 P2 验收标准

- 批量修复可精准限定指定页面范围，不影响其他页面控件；
- 支持单页面控件独立版本导出，文件数据完整；
- 页面依赖统计明细准确，可完整展示页面所有控件引用关系；
- 页面各项统计数据（数量、状态、命中次数）实时准确。

## 8. 业务价值

- **迭代提效**：页面改版仅需维护对应分组控件，无需全局检索，杜绝遗漏；
- **协作隔离**：不同人员负责不同页面，筛选隔离操作，互不干扰；
- **规范管控**：统一页面命名体系，解决字段杂乱、不统一问题；
- **风险可控**：改版前通过页面依赖评估全局脚本影响范围，规避线上风险；
- **整洁运维**：统一归集无归属零散控件，定期批量清理冗余数据。

## 9. 后续扩展规划（可选）

- 支持二级页面分组：实现「首页-子页面」层级化管控；
- 新增页面权限：按用户分配页面查看/操作权限，精细化权限管控；
- 新增页面操作日志：记录页面迁移、归属修改、批量运维操作记录，可追溯。
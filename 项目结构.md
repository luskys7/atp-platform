# 项目结构

自动化测试平台-1/
├── backend/              # Golang 后端（Gin + GORM + Redis + MinIO）
│   ├── cmd/server/       # 服务入口
│   └── internal/         # 配置、模型、服务、Handler、路由、中间件
├── frontend/             # Vue3 + Element Plus 管理后台
│   └── src/views/        # P0 全部页面
├── agent/                # 设备 Agent 驻留进程
├── migrations/           # P0 + P1 数据库 SQL
├── scripts/              # 部署脚本
├── docker-compose.yml    # 一键部署
└── README.md



## 已实现模块（对齐文档）

| 模块       | 实现内容                                                |
| :--------- | :------------------------------------------------------ |
| 设备管理   | 白名单校验(E1001)、注册/心跳、台账、状态管理            |
| 并行调度   | Redis 分布式锁(E1002)、任务队列(E3001)、超时熔断(E3002) |
| 自动化执行 | Appium/Python 脚本、并行拆分、执行实例                  |
| 录屏       | MinIO 存储、水印哈希、回放 URL(E2001/E2002)             |
| 日志报告   | 执行日志聚合、报告生成、PDF 导出                        |
| 权限       | JWT 鉴权、四级角色、操作审计                            |
| P1 控件池  | 全局控件池、步骤级私有绑定(E4001-E4003)                 |



## 快速启动

### Docker 一键部署
docker-compose up -d --build

### 或 Windows 下运行
scripts\deploy.bat

- 前端：[http://localhost](http://localhost/)
- 后端：[http://localhost:8080](http://localhost:8080/)
- 默认账号：`admin` / `admin123`

## 
## 1. 文档目的

统一移动端（Android/iOS）UI自动化测试元素定位选型标准，解决自动化脚本随意选择定位方式导致的**用例不稳定、维护量大、版本迭代易失效、机型适配性差**等问题。提供唯一、可落地、可强制执行的选型决策逻辑，适用于移动端自动化平台开发、测试人员、自动化脚本编写人员。

## 2. 核心选型总原则（移动端统一最高准则）

所有移动端元素定位选型，严格遵循以下四项优先级原则，所有场景均需遵从：

1. **稳定性第一**：优先选择不受APP迭代更新、UI改版、多语言适配、样式调整、机型适配影响的定位方式。
2. **执行效率第二**：优先移动端原生高性能定位（UiSelector / NSPredicate），规避XPath、图像识别等低效、高延迟方案。
3. **可维护性第三**：定位表达式简洁、辨识度高、通用性强，减少版本迭代后的修改频次。
4. **降级兜底必备**：核心业务元素必须配置「主定位 + 备选定位」双策略，禁止单一定位裸跑，降低用例失败率。

## 3. 标准选型决策流程（移动端强制流程）

移动端元素定位选型必须按以下顺序执行，禁止跳过任意步骤，确保定位稳定有效。

### 步骤1：判断元素载体与上下文（定位生效前提）

载体和上下文判断错误，所有定位规则全部失效，优先确认元素所属场景：

- Android/iOS 原生APP控件 → 对应端原生定位体系
- APP内嵌H5（WebView） → 切换WEBVIEW上下文，启用H5专属定位
- 系统弹窗、权限弹窗、通知弹窗 → 切换系统独立上下文
- Toast浮层、无控件树提示文本 → 依托屏幕快照+OCR兜底定位
- 游戏、画布、第三方无SDK视图 → 直接启用视觉兜底定位

### 步骤2：优先使用【自动化专属预埋标识】（移动端最高优先级）

开发预埋专属自动化标识，是移动端最稳定、零维护的首选定位方案，优先级高于所有通用定位方式。

- **Android端**：稳定唯一 resourceId > content-desc（无障碍标识/accessibilityId）
- **iOS端**：accessibilityIdentifier（行业标准自动化专属标识）

元素存在合规预埋标识，直接优先使用，无需考虑其他定位方式；无预埋标识则进入下一步选型。

### 步骤3：选用移动端原生高性能定位（次优标准方案）

无预埋自动化标识时，优先使用移动端系统原生底层定位，性能、稳定性远超XPath等通用方案。

- **Android端首选：UiSelector组合定位**
  - 安卓系统原生底层查询方案，执行性能、匹配速度远超XPath
  - 支持resourceId、文本、控件类名、正则多条件组合匹配
  - 内置长列表自动滚动、循环查找能力，适配动态加载列表
- **iOS端首选：NSPredicate**
  - iOS官方原生高性能定位方案，是iOS最优核心定位方式
  - 支持多条件叠加、模糊匹配、正则匹配、控件状态筛选
  - 复杂层级页面可使用Class Chain替代XPath，提升稳定性和执行速度

### 步骤4：无稳定属性，使用【复合/限定定位】（复杂页面标准解法）

禁止单独使用文本、控件类名、索引做主定位，必须叠加限定条件，规避元素匹配错乱问题，移动端复杂页面强制使用：

- **容器限定定位（列表/表格强制使用）**：先锁定外层父容器（RecyclerView、UICollectionView、TableView等），仅在容器内匹配子元素，解决页面同类控件重名、批量匹配问题。
- **相对关系定位**：依托相邻文本标签、兄弟控件、上下层级关系定位，不依赖固定控件路径，适配UI微调迭代场景。
- **多条件组合定位**：控件类型+文本+属性组合匹配，大幅提升元素唯一性。
- **状态过滤叠加**：筛选可见、可点击、非禁用、选中状态的有效元素，过滤隐藏、失效脏元素。

### 步骤5：页面结构复杂，使用XPath兜底（仅备选、禁止做主定位）

仅原生定位、复合定位无法实现的复杂层级、特殊逻辑场景使用，作为降级备选方案，严格遵守约束规则：

- 禁止使用绝对XPath，仅允许相对XPath
- 层级深度≤3层，过长层级极易因UI迭代失效
- 必须配套主定位策略，仅作为自动降级备选，不单独使用

### 步骤6：无控件树场景，启用视觉兜底定位（最低优先级）

上述所有定位方案均失效时启用，仅作为极端场景兜底，禁止作为常规用例主定位：OCR文字识别、图像模板匹配、百分比比例坐标。

## 4. 移动端分场景精准选型规范（强制执行）

### 4.1 常规按钮、输入框、核心业务控件

- Android：resourceId > accessibilityId > UiSelector多条件组合定位
- iOS：accessibilityIdentifier > NSPredicate多条件定位

禁止：单独使用控件文本、单独类名、单独索引做主定位。

### 4.2 列表、表格、动态分页加载数据行

**强制规范：容器限定 + 行内容匹配 + 自动翻页滚动查找**

禁止全局直接匹配按钮、文本元素，避免多列表、同类型控件匹配错乱。

### 4.3 多语言APP、运营活动页、文案频繁变更页面

**严禁使用文本、label、text作为主定位条件**，仅可使用预埋标识、原生ID、容器限定、相对关系定位，规避多语言文案替换导致的用例失效。

### 4.4 APP内嵌H5（WebView）页面

优先切换WEBVIEW上下文，切换后启用H5适配定位规则，禁止直接使用移动端原生控件定位方式。

### 4.5 带状态控件（开关、复选框、滑块、Tab切换）

定位表达式必须叠加状态过滤条件，通过value、selected、checked、可用状态筛选精准元素，避免匹配到默认状态控件。

### 4.6 系统弹窗、权限弹窗、Toast浮层

此类元素脱离APP原生控件树，常规定位无效，需切换系统上下文，搭配OCR文字识别完成校验和适配，不使用常规控件定位方案。

### 4.7 画布、游戏、无SDK第三方视图

无标准控件树，唯一适配方案：OCR文字识别+图像模板匹配双层降级兜底。

## 5. 移动端定位方式禁用/慎用/推荐清单

### 5.1 优先推荐（稳定、高效、平台主推）

resourceId、accessibilityIdentifier、content-desc、UiSelector、NSPredicate、Class Chain、容器复合定位、相对关系定位

### 5.2 谨慎使用（仅备选，禁止做主定位）

XPath、纯文本Text/Label、控件类名+Index索引定位

### 5.3 严格禁止（正式用例完全不允许）

绝对XPath、固定像素坐标、单图像匹配做主定位、纯索引定位、纯文本主定位

## 6. 移动端平台自动化辅助规范

自动化平台需内置移动端专属智能选型能力，降低人为选型失误，提升用例稳定性：

1. **自动推荐最优移动端定位**：抓取元素后，自动生成移动端主定位+备选降级定位组合
2. **定位风险智能校验**：识别动态混淆ID、纯文本定位、超长XPath等风险并预警
3. **上下文自动识别切换**：自动区分原生控件、WebView、系统弹窗上下文
4. **多级策略降级配置**：可视化编辑移动端元素多级定位降级规则
5. **定位稳定性评分**：对移动端定位表达式打分，标注维护风险等级

## 7. 移动端极简选型口诀（团队宣导落地）

预埋标识优先级高，原生定位效率优； 列表先锁父容器，文案不做主参数； 多语言避文本匹配，弹窗切换新上下文； XPath仅做备用项，视觉坐标最后兜底； 双策降级稳用例，迭代少修易维护。
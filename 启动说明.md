# TestFlow 本地启动说明

项目根目录：`c:\Users\yu.xiao03\UI_AutoTest\atp-platform-main\atp-platform-main`

---

## 一、一键启动（推荐）

> 必须在项目根目录执行（目录下能看到 `scripts/`、`frontend/`、`backend-java/`）。  
> 错误示例：在上一级 `atp-platform-main` 里运行会找不到脚本。

```powershell
Set-ExecutionPolicy -Scope Process Bypass
cd "c:\Users\yu.xiao03\UI_AutoTest\atp-platform-main\atp-platform-main"
.\scripts\start-local.ps1
```

| 操作 | PowerShell | 或双击 |
|------|------------|--------|
| 启动 | `.\scripts\start-local.ps1` | `scripts\start-local.bat` |
| 停止 | `.\scripts\stop-local.ps1` | `scripts\stop-local.bat` |

脚本行为：

1. 释放占用中的 `3000 / 8080 / 9002`
2. 清理 H2 数据库锁文件（避免后端卡住起不来）
3. 启动后端 `mvn spring-boot:run`，**轮询健康检查直到就绪**（最长约 90 秒）
4. 启动执行器 `:9002`
5. 启动前端 `:3000`，确认可访问后再提示打开页面

请等到脚本打印 `All ready` / `[OK] Backend ready` 后再打开浏览器；后端未就绪时打开页面会出现长时间 loading。

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端 | http://localhost:3000 | 管理后台（请从这里访问） |
| 后端 API | http://localhost:8080/api/v1/health | REST API，不要当网页直接打开 |
| 执行器 | http://localhost:9002/health | Python 自动化执行节点 |
| 默认账号 | `admin` / `admin123` | 登录凭据 |

---

## 二、分步手动启动

### 0. 环境变量（新开 PowerShell 建议先执行）

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot"
cd "e:\自动化测试平台-1"
```

**环境要求**：JDK 17+ · Node.js 18+ · Python 3.11+ · 无需 Docker

### 1. 编译后端（首次启动或修改 Java 代码后）

```powershell
& "e:\自动化测试平台-1\tools\apache-maven-3.9.6\bin\mvn.cmd" `
  -f "e:\自动化测试平台-1\backend-java\pom.xml" `
  -DskipTests package
```

### 2. 启动后端（:8080）

```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
cd "c:\Users\yu.xiao03\UI_AutoTest\atp-platform-main\atp-platform-main\backend-java"
java -jar target\atp-platform-1.0.0.jar --spring.profiles.active=local
```

新开窗口后台启动：

```powershell
Start-Process cmd -ArgumentList "/k", "cd /d `"e:\自动化测试平台-1\backend-java`" & java -jar target\atp-platform-1.0.0.jar --spring.profiles.active=local"
```

### 3. 启动 Python 执行器（:9002）

```powershell
cd "e:\自动化测试平台-1\executor"
python -m pip install -q fastapi "uvicorn[standard]" websockets pydantic
python main.py
```

自定义端口：

```powershell
$env:EXECUTOR_PORT = "9002"
python main.py
```

### 4. 启动前端（:3000）

首次需安装依赖：

```powershell
cd "e:\自动化测试平台-1\frontend"
npm install
npm run dev
```

生产构建预览：

```powershell
npm run build
npm run preview
```

### 5. 启动设备 Agent（可选，真机注册/心跳）

> **local 模式已默认开启 USB 自动发现**：插上 USB 并 `adb devices` 可见后，约 10 秒内自动出现在设备池，**无需手动加白名单、无需启动 Agent**。  
> 也可在「设备管理」页点击 **同步 USB 设备** 立即刷新。

远程设备或非本机 ADB 时，仍使用 Agent：

```powershell
cd "e:\自动化测试平台-1\agent"
python -m pip install -q requests pyyaml
python main.py
```

配置文件：`agent\config\config.yaml`（默认连接 `http://localhost:8080`）

---

## 三、健康检查

```powershell
# 后端
Invoke-WebRequest http://localhost:8080/api/v1/health -UseBasicParsing

# 执行器
Invoke-WebRequest http://localhost:9002/health -UseBasicParsing

# 登录测试
$body = '{"username":"admin","password":"admin123"}'
Invoke-WebRequest http://localhost:8080/api/v1/auth/login `
  -Method POST -Body $body -ContentType "application/json" -UseBasicParsing
```

---

## 四、停止与重启

### 查看端口占用

```powershell
Get-NetTCPConnection -LocalPort 8080,3000,9002 -ErrorAction SilentlyContinue |
  Select-Object LocalPort, OwningProcess, State
```

### 结束进程

```powershell
# 按 PID 结束（将 12345 替换为实际 PID）
Stop-Process -Id 12345 -Force

# 结束所有 Java 进程（慎用，会关闭本机全部 Java）
Get-Process java -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 重启后端（完整流程）

```powershell
# 1. 结束占用 8080 的进程
$p = (Get-NetTCPConnection -LocalPort 8080 -ErrorAction SilentlyContinue).OwningProcess | Select-Object -First 1
if ($p) { Stop-Process -Id $p -Force; Start-Sleep -Seconds 2 }

# 2. 重新编译
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot"
& "e:\自动化测试平台-1\tools\apache-maven-3.9.6\bin\mvn.cmd" `
  -f "e:\自动化测试平台-1\backend-java\pom.xml" -DskipTests package

# 3. 启动
cd "e:\自动化测试平台-1\backend-java"
java -jar target\atp-platform-1.0.0.jar --spring.profiles.active=local
```

---

## 五、验证脚本（改代码后）

```powershell
cd "e:\自动化测试平台-1"
python scripts\verify-batch14.py
# 其他批次：verify-batch9.py ~ verify-batch13.py
```

改 Java 后需 `mvn package` 并重启 JAR；改 Python 后需重启执行器。

---

## 六、端口与服务对照

| 端口 | 服务 | 目录 |
|------|------|------|
| 3000 | Vue3 前端 | `frontend/` |
| 8080 | Spring Boot 后端 | `backend-java/` |
| 9002 | Python 执行器 | `executor/` |
| 9100 | 设备 Agent | `agent/` |
| 8100 | iOS WDA（设备侧） | 由 WDA 部署功能拉起 |

---

## 七、常见问题

1. **前端提示「后端不可用」**  
   先确认 `http://localhost:8080/api/v1/health` 返回 `{"status":"ok"}`，再刷新 `http://localhost:3000`。

2. **浏览器打开 `:8080` 白屏或 403**  
   正常，后端是 API 服务；请访问 `http://localhost:3000`。

3. **`Port 8080 was already in use`**  
   旧后端仍在运行，先结束占用进程再启动（见第四节）。

4. **修改 Java 代码不生效**  
   必须重新 `mvn package` 并重启 JAR，热更新无效。

5. **修改 Python 执行器不生效**  
   重启 `executor/main.py` 进程。

6. **H2 数据库控制台（local 模式）**  
   http://localhost:8080/h2-console  
   JDBC URL：`jdbc:h2:file:./data/atp_local`

7. **前端提示 `Request failed with status code 403`**  
   常见原因：登录 Token 过期或无效。请退出后重新登录（`admin / admin123`）。  
   若仍出现，按 F12 打开开发者工具 → Network，查看具体是哪个 API 返回 403；  
   - 401 / 未登录 → 重新登录  
   - 403 权限不足 → 当前账号角色不够（如 CI 配置需管理员）  
   - 403 IP 白名单 → 检查 `application.yml` 中 `atp.security.ip-whitelist-enabled`

---

## 九、远程访问 + 本机设备（方案 B）

中央机器跑前端/后端，访问者本机只跑执行器并插手机。

### 中心侧（管理员）

1. 启动平台（`.\scripts\start-local.ps1`），放行防火墙 `3000` / `8080`
2. **首次**（或启动器代码有更新后）打包访客启动器 exe，供网页下载：

```powershell
cd "c:\Users\yu.xiao03\UI_AutoTest\atp-platform-main\atp-platform-main"
.\scripts\build-executor-exe.ps1
```

产物：`backend-java\data\downloads\TestFlow-Executor.exe`  
打包后如后端已在跑，一般无需重启即可下载新文件；若下载 API 是新加的，需先重启后端。

3. 同事浏览器访问：`http://中心局域网IP:3000`

### 访问者侧：本机启动器（推荐，无需源码）

#### 前置条件

- 本机已安装 **ADB**，并加入 PATH（命令行执行 `adb version` 能成功）
- 手机开启 **USB 调试**（部分机型还需「USB 调试（安全设置）」）
- 本机与中心机在同一局域网；防火墙放行本机执行器端口（默认 **9002**）
- **不要**与本机已运行的 `start-local.ps1` 抢同一端口（中心机自己插手机用「同步 USB」即可，不必再开启动器）

#### 下载与启动

1. 浏览器登录平台 → **设备管理** → 点击「同步 USB」左侧的 **下载启动器**
2. 得到 `TestFlow-Executor.exe`，双击运行
3. 填写界面信息后点 **启动执行器**：

| 字段 | 填写示例 | 说明 |
|------|----------|------|
| 中心平台地址 | `http://10.0.0.5:8080` | 中央后端地址，须带 `http://`，不要填前端 3000 端口 |
| 本机端口 | `9002` | 本机监听端口，被占用时可改 |
| 本机对外地址 | `http://10.0.0.12:9002` | 须用**局域网 IP**，不要用 `127.0.0.1`（中心机需能访问到你） |

4. 保持窗口打开；约 **10 秒**后，手机应出现在平台「设备管理」列表（可看 `executor_url` 是否指向你的本机）

#### 重新启动（改地址 / 启动失败后）

启动失败、填错中心地址、或换了端口时：

1. 直接修改上方三个字段（运行中也可改）
2. 点击 **重新启动**（失败或曾启动过后，原「启动执行器」会变成此按钮）
3. 启动器会先停止旧进程，再按新配置启动

也可先点 **停止**，确认状态变为已停止后再改信息、点「重新启动」。

常见失败原因：

| 现象 | 处理 |
|------|------|
| 端口已被占用 | 换端口，或结束本机占用 9002 的旧执行器 / 不要同时跑 `start-local` |
| 中心地址无效 | 确认中心 `http://中心IP:8080/api/v1/health` 可访问 |
| 设备不上报 | 检查 ADB、USB 调试、本机对外 IP、中心防火墙是否放行访问你的 9002 |
| 中心无法投屏/控制 | 确认「本机对外地址」是中心机能 ping/访问到的局域网 IP |

中心地址会写入启动器同目录的 `executor_launcher.ini`，下次打开会自动带出。

### 访问者侧（备选：有源码时）

```powershell
.\scripts\start-remote-executor.ps1 -PlatformUrl http://中心IP:8080 -PublicUrl http://本机IP:9002
```

请把「中心IP」「本机IP」换成真实数字 IP，不要照抄字面量。

环境变量说明：

| 变量 | 含义 |
|------|------|
| `PLATFORM_URL` | 中央后端，如 `http://10.0.0.5:8080` |
| `EXECUTOR_PUBLIC_URL` | 本执行器对外地址，如 `http://10.0.0.12:9002` |
| `EXECUTOR_PORT` | 本地监听端口，默认 9002 |

---

## 十、相关文档

- [README.md](./README.md) — 项目概览
- [自动化测试平台.md](./自动化测试平台.md) — 完整研发文档
- [平台核心.md](./平台核心.md) — 需求与优先级

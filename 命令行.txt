# ========== 一键启停（推荐）==========
# 必须在含 scripts/ 的项目根目录：
#   c:\Users\yu.xiao03\UI_AutoTest\atp-platform-main\atp-platform-main

Set-ExecutionPolicy -Scope Process Bypass
cd "c:\Users\yu.xiao03\UI_AutoTest\atp-platform-main\atp-platform-main"

# 启动（会先停旧进程 → 等后端健康 → 再开前后端）
.\scripts\start-local.ps1

# 停止
.\scripts\stop-local.ps1

# 或双击：
#   scripts\start-local.bat
#   scripts\stop-local.bat

# 访问：http://localhost:3000   账号 admin / admin123

# ========== 远程执行器（访问者本机）==========
# .\scripts\start-remote-executor.ps1 -PlatformUrl http://中心IP:8080 -PublicUrl http://本机IP:9002

# ========== 仅启动后端（备用）==========
# $env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
# cd backend-java
# ..\tools\apache-maven-3.9.6\bin\mvn.cmd -DskipTests spring-boot:run "-Dspring-boot.run.profiles=local"

# ========== 仅启动前端（备用）==========
# $env:Path = "C:\Program Files\nodejs;" + $env:Path
# cd frontend
# npm run dev
